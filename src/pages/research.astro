---
import { getCollection } from "astro:content";
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
const allProjects = await getCollection("projects");
const pageTitle = "Research";
---
<Layout pageTitle={pageTitle}>
    <div class="gif">
        <img width="300" height="250" alt="RaccoonLaptopGif" src="/laptop.webp"/>
    </div>
    <h2>My Research</h2>
    <div>
    </div>
    <p>
        This section will eventually be overhauled once dissertation research starts. For now, these pages have some information about a few undergraduate research projects I worked on.
        For a complete list of experiences, see my CV below. 
    </p>
    <ul role="list" class="link-card-grid">
        {allProjects.map((card) => <Card 
            url={`/projects/${card.slug}/`} 
            image={card.data.image.url} 
            title={card.data.title} 
            body={card.data.description}
        />)}
    </ul>
    <div></div>
    <h2>My CV </h2> 

    <!-- <div></div>
    <div></div> -->

    <div class="pdf-container">
    <embed class="pdf-embed" src="/vitaesun.pdf" type="application/pdf" width="100%" height="600px" />
    </div>
    <p>
        If the PDF preview does not work (esp. on mobile), click
        <a href="/vitaesun.pdf" target="_blank">here</a>
        to open in a new tab.
    </p>
    
    <style>
        .pdf-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 80vh; /* Adjust height as needed */
        }
        .pdf-embed {
        width: 100%; /* Adjust width as needed */
        max-width: 800px; /* Example maximum width */
        height: 100%;
        }   
    </style>

    


</Layout>

